# Database
DATABASE_URL="postgresql://braingym:braingym_dev_2024@localhost:5432/braingym?schema=public"

# JWT
JWT_SECRET="braingym-jwt-secret-change-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="braingym-refresh-secret-change-in-production"
JWT_REFRESH_EXPIRES_IN="7d"

# Redis
REDIS_HOST="localhost"
REDIS_PORT=6379

# App
PORT=3000
NODE_ENV=development

# AI Question Bank — Key Encryption (generate with: openssl rand -hex 32)
# Must be at least 32 characters. Change this in production!
LLM_KEY_ENCRYPTION_SECRET="braingym-llm-encryption-secret-change-in-production"
